// ============================================================
// /capture — Webcam Capture Station — DEV B owns this file
// ============================================================

import { CaptureStation } from '@/components/CaptureStation';

export default function CapturePage() {
  return (
    <main className="min-h-screen bg-black">
      <CaptureStation />
    </main>
  );
}
