// ============================================================
// faceSlots — Pre-defined face positions in the scene — DEV A
// ============================================================
// Generate 40-50 slots in 4-5 rows (graduation photo layout).
// Back rows: smaller scale. Front rows: larger.
// Slight random offset to avoid rigid grid look.
// Pre-populate ~20 slots with famous faces, leave 20-30 for guests.
//
// See SPEC.md § "FaceSlot System" for details.

import type { FaceSlot } from "@/types";

// TODO (Dev A): Generate face slot positions
export type { FaceSlot };
